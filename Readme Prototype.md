# Реалізація патернів "Прототип" і "Будівельник"

# 📚 Книжковий магазин

## 📖 Опис
Це програмне забезпечення призначене для управління книжковим магазином, що включає зберігання інформації про книги, замовлення та користувачів. Реалізація здійснюється з використанням породжуючих патернів проектування.

Система дозволяє користувачам переглядати доступні книги, оформлювати замовлення, а адміністраторам — керувати каталогом книг та обробляти замовлення. Основна мета – створити ефективну та масштабовану архітектуру з використанням патернів проектування.

---

## 🟢 Реалізація патерну "Прототип" (Перший рівень)

### 🔍 Завдання
Написати програмне забезпечення, що використовує породжуючий патерн **Прототип (Prototype)** для створення об'єктів книг та їх копіювання.

### 👨‍💼 Опис реалізації
- **Книга (Book)**
  - Назва
  - Автор
  - Ціна
  - Жанр
  - ISBN
- **Прототип книги**
  - Створення копії книги за допомогою методу `clone()`.

### 🔗 Приклад використання
1. Створення екземпляра книги.
2. Клонування книги для створення копії.
3. Перевірка ідентичності властивостей між оригіналом і копією.

### 🎯 Переваги використання
- Зменшення витрат на створення нових об'єктів.
- Полегшення процесу дублювання об'єктів.
- Гнучкість у зміні параметрів копій об'єктів.

---

## 🟡 Декомпонування завдання та використання патерну "Прототип" (Другий рівень)

### 🌐 Технічне завдання
Розробити систему управління книжковим магазином з розподілом функціональних компонентів.

### 🛠️ Декомпонування завдання
- **Керування книгами**
  - Додавання нових книг
  - Клонування книг (Прототип)
  - Редагування інформації про книги
  - Видалення книг
- **Користувачі**
  - Реєстрація
  - Перегляд доступних книг
  - Оформлення замовлень
- **Адміністратор**
  - Управління каталогом книг
  - Перегляд замовлень

### 🔧 Реалізація патерну
- Патерн **Прототип** використовується для швидкого копіювання книг, щоб уникнути створення об'єктів з нуля.

### 🚀 Очікуваний результат
- Поліпшення продуктивності при створенні нових екземплярів книг.
- Можливість швидкого дублювання популярних книг.
- Гнучкість у модифікації клонованих книг.

---

## 🔵 Декомпонування завдання та використання патернів "Прототип" і "Будівельник" (Третій рівень)

### 🛠️ Технічне завдання
Розширити функціонал, додавши додатковий породжуючий патерн **Будівельник (Builder)** для створення складних об'єктів книг з багатьма параметрами.

### 🔢 Декомпонування завдання
- **Керування книгами** (Прототип, Будівельник)
- **Користувачі**
- **Замовлення**
- **Адміністратор**

### 🔧 Реалізація патернів
- **Прототип (Prototype)** – для клонування книг.
- **Будівельник (Builder)** – для покрокового створення об'єктів книг, враховуючи можливість конфігурації параметрів (назва, жанр, автор, видавництво, формат, мова тощо).

### 💡 Додаткові можливості
- Покрокове створення складних об'єктів книг.
- Розширюваність системи для підтримки різних форматів книг (електронні, друковані).
- Гнучкість у налаштуванні параметрів книг без створення громіздких конструкторів.

### 🏆 Очікуваний ефект
- Оптимізація процесу створення складних об'єктів.
- Спрощення розширення функціоналу книжкового магазину.
- Покращення підтримки та супроводження коду в довгостроковій перспективі.

---

## 2. Реалізація патерна "Прототип"

### 2.1. Код
```python
import copy

class BookPrototype:
    """ Прототип книги """
    def __init__(self, title, author, price):
        self.title = title  # Назва книги
        self.author = author  # Автор книги
        self.price = price  # Ціна книги

    def clone(self):
        """ Створює глибоку копію книги """
        return copy.deepcopy(self)

    def get_info(self):
        """ Повертає інформацію про книгу """
        return f'Книга: "{self.title}" - {self.author}, ${self.price:.2f}'

# Створення початкового екземпляра книги
original_book = BookPrototype("1984", "Джордж Орвелл", 12.99)

# Клонування книги
cloned_book = original_book.clone()
cloned_book.price = 9.99  # Зміна ціни для копії

# Виведення інформації
print(original_book.get_info())  # Книга: "1984" - Джордж Орвелл, $12.99
print(cloned_book.get_info())    # Книга: "1984" - Джордж Орвелл, $9.99
```

### 2.2. Пояснення коду

## Клас `BookPrototype`

- **Конструктор `__init__`** ініціалізує об'єкт книги з трьома параметрами: `title` (назва), `author` (автор) і `price` (ціна).
- **Метод `clone()`** створює **глибоку копію** (`deepcopy`) об'єкта, що гарантує копіювання вкладених структур.
- **Метод `get_info()`** повертає відформатований рядок з інформацією про книгу.

## Створення початкового екземпляра

```python
original_book = BookPrototype("1984", "Джордж Орвелл", 12.99)
```
Це оригінальний об'єкт, що містить інформацію про книгу "1984".

## Клонування

```python
cloned_book = original_book.clone()
```
Тепер `cloned_book` є новим об'єктом, який містить ті ж дані, що й `original_book`.

## Зміна атрибута `price` у клоні

```python
cloned_book.price = 9.99
```
Це не впливає на `original_book`, оскільки `clone()` створює **повністю незалежний об'єкт**.

## Перевірка результату

```python
print(original_book.get_info())  # Книга: "1984" - Джордж Орвелл, $12.99
print(cloned_book.get_info())    # Книга: "1984" - Джордж Орвелл, $9.99
```
Як бачимо, зміна ціни вплинула лише на `cloned_book`, а оригінал залишився незмінним.

---

## 3. Декомпозиція завдання з використанням "Прототипа"

У цьому рівні додаємо **клас каталогу**, що дозволяє зберігати список книг і взаємодіяти з ними.

### 3.1. Код
```python
class Catalog:
    """ Каталог книг """
    def __init__(self):
        self.books = []  # Ініціалізація списку книг
    
    def add_book(self, book):
        """ Додає книгу до каталогу """
        self.books.append(book)
    
    def list_books(self):
        """ Повертає список інформації про всі книги """
        return [book.get_info() for book in self.books]

# Використання каталогу
catalog = Catalog()
catalog.add_book(original_book)
catalog.add_book(cloned_book)

for book in catalog.list_books():
    print(book)
```

## 3.2. Пояснення коду

## **Клас `Catalog`**
Клас `Catalog` використовується для управління колекцією книг. Він дозволяє додавати книги до каталогу та отримувати список усіх збережених книг.  
Завдяки цьому класу можна створити централізовану систему зберігання та керування книжковими записами.

## **Метод `__init__()`**
Метод ініціалізації `__init__()` створює новий екземпляр класу `Catalog`.  
При створенні об'єкта каталогу (`catalog = Catalog()`) автоматично створюється порожній список `self.books`,  
який слугує сховищем для об'єктів книг.  

**Приклад:**  
```python
catalog = Catalog()  # Створення порожнього каталогу
print(catalog.books)  # Виведе: []
```
Цей список буде заповнюватися екземплярами книг у майбутньому.

## Метод `add_book(book)`
Метод `add_book(book)` приймає об'єкт книги та додає його до списку `self.books`.
Цей метод дозволяє зберігати в каталозі різні книги, які можуть бути як оригінальними, так і клонованими копіями.

Приклад:
```
catalog.add_book(original_book)  # Додаємо книгу
catalog.add_book(cloned_book)  # Додаємо клон книги
```
Після виконання цих команд список `self.books` міститиме два об'єкти книг.

##  Метод list_books()
Метод `list_books()` проходить по всіх книгах у списку `self.books` та повертає їх інформацію у вигляді списку.
Він використовує метод `book.get_info()` кожного об'єкта книги, щоб отримати опис книги.

Приклад:
```
for book in catalog.list_books():
    print(book)  # Виведе інформацію про всі книги в каталозі
Тобто, якщо в каталозі є дві книги, результат буде таким:

Назва: "Гаррі Поттер", Автор: "Дж. К. Ролінг"
Назва: "Гаррі Поттер (Копія)", Автор: "Дж. К. Ролінг"
```
Цей метод корисний для перегляду вмісту каталогу та подальшої обробки даних.

## Загальний підсумок
Клас Catalog дозволяє:
- Зберігати книги у структурованому списку.
- Додавати книги до каталогу.
- Отримувати список усіх книг з інформацією про них. Таким чином, він забезпечує зручний механізм роботи з книгами в рамках патерна "Прототип".

---

## 4. Використання патернів "Прототип" та "Будівельник"

На цьому етапі додаємо можливість **створювати книги поетапно** за допомогою "Будівельника".

### 4.1. Код
```python
class BookBuilder:
    """ Будівельник для створення книги """
    def __init__(self):
        self.reset()
    
    def reset(self):
        """ Очищує створення нового об'єкта книги """
        self._book = BookPrototype("", "", 0.0)
    
    def set_title(self, title):
        """ Встановлює назву книги """
        self._book.title = title
        return self
    
    def set_author(self, author):
        """ Встановлює автора книги """
        self._book.author = author
        return self
    
    def set_price(self, price):
        """ Встановлює ціну книги """
        self._book.price = price
        return self
    
    def build(self):
        """ Завершує створення об'єкта книги """
        book = self._book
        self.reset()
        return book

# Використання будівельника
builder = BookBuilder()
new_book = (builder.set_title("Гаррі Поттер")
                   .set_author("Дж. Роулінг")
                   .set_price(20.99)
                   .build())

print(new_book.get_info())  # Книга: "Гаррі Поттер" - Дж. Роулінг, $20.99
```

### 4.2. Пояснення коду

## **Клас `BookBuilder`**
Клас `BookBuilder` реалізує патерн "Будівельник", який дозволяє створювати об'єкти книги поетапно.  
Він забезпечує гнучкість у процесі створення книги, дозволяючи змінювати її параметри без необхідності передавати всі аргументи відразу.

## **Метод `__init__()`**
Метод `__init__()` викликає `reset()`, що створює порожній об'єкт книги (`BookPrototype`).  
Цей підхід дозволяє гарантувати, що кожен будівельник починає роботу з "чистого листа".

Приклад:
```python
builder = BookBuilder()  # Ініціалізація будівельника
Після цього об'єкт builder готовий до поетапного налаштування книги.
```
## Метод `reset()`
Метод `reset()` створює новий екземпляр книги BookPrototype з порожніми значеннями.
Цей метод викликається як під час ініціалізації, так і після завершення побудови книги (build()).

Приклад:
```
builder.reset()  # Очищує поточний стан будівельника
```
Це дозволяє багаторазово використовувати один екземпляр BookBuilder для створення різних книг.

## Методи `set_title(title), set_author(author), set_price(price)`
Ці методи дозволяють поступово встановлювати властивості книги:
```
set_title(title) – задає назву книги.
set_author(author) – встановлює автора книги.
set_price(price) – визначає її ціну.
```
Кожен із цих методів повертає той самий об'єкт будівельника (self),
що дозволяє використовувати ланцюжковий виклик методів (method chaining).

Приклад:
```
builder.set_title("Гаррі Поттер").set_author("Дж. Роулінг").set_price(20.99)
```
Це спрощує процес створення книги, дозволяючи записувати його в одному рядку.

## Метод `build()`
Метод `build()` завершує процес створення книги:

Повертає поточний екземпляр `self._book`.
Викликає reset(), щоб підготувати будівельник до створення наступної книги.
Приклад:
```
new_book = builder.build()  # Отримуємо готовий об'єкт книги
```
Цей підхід дозволяє створювати кілька книг поспіль без необхідності щоразу створювати новий BookBuilder.

Приклад використання будівельника
```
builder = BookBuilder()
new_book = (builder.set_title("Гаррі Поттер")
                   .set_author("Дж. Роулінг")
                   .set_price(20.99)
                   .build())

print(new_book.get_info())  # Виведе: Книга: "Гаррі Поттер" - Дж. Роулінг, $20.99
```
Ініціалізується будівельник.
Встановлюються параметри книги.
Викликається build(), що повертає новий об'єкт книги.
Використовується get_info(), щоб отримати опис книги.

## Загальний підсумок
Клас BookBuilder дозволяє:
- Гнучко створювати книги, змінюючи їх параметри окремо.
- Використовувати ланцюжковий виклик (set_title(...).set_author(...).set_price(...)).
- Автоматично очищати будівельник після завершення (reset() після build()).

---

## 5. Висновки

У цій роботи реалізували:
1. **Патерн "Прототип"**, що дозволяє швидко створювати копії об'єктів.
2. **Патерн "Будівельник"**, що дає змогу покроково конфігурувати складні об'єкти.
3. **Декомпозицію завдання**, що покращує підтримку та гнучкість коду.

🚀 Завдяки цим підходам, код став **масштабованим** і **зручним для розширення**!
